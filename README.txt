-Couldn't get the Porter stemming algorithm to work. However i did part i and ii properly.
-My clusters algorithm works however it doesn't print to the folder properly.
-Couldn't print out the merges properly but there is a clusters arrayList that contains all the merges in order. 
-Didn't do the dendrogram. 
